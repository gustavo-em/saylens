/** User-facing release version, injected from VERSION_NAME in the root .env. */
export const APP_VERSION = __LESINGO_VERSION__;

/** Replaced by Babel from LESINGO_SHOW_DIAGNOSTICS in the root .env. */
export const DIAGNOSTICS_ENABLED = __LESINGO_DIAGNOSTICS__;
