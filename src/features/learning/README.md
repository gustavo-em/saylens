# Learning feature

This bounded feature owns the complete journey from a detected object to an
English-learning overlay that remains visible in the live camera context.

The feature is split into `domain`, `application`, `infrastructure`, and
`presentation`. Dependencies must follow the repository architecture document.
