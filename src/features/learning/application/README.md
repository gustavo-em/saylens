# Application

Use cases and external capability ports live here.

This layer may depend on the learning domain. It must not depend on concrete
camera, detector, dictionary, speech, storage, navigation, or UI implementations.
