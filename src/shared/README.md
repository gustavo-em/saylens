# Shared

This folder is reserved for small, proven cross-feature primitives such as
design-system components, error utilities, and test helpers.

Feature-specific code stays inside its feature. Nothing is promoted here before
it has at least two real consumers.
